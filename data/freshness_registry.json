{
  "$comment": "Machine-readable freshness registry — the single source of truth for WHEN every data class is meant to refresh and how stale it is allowed to get. Consumed by scripts/check_freshness.mjs (CI/owner check), lib/freshness.ts (the /api/freshness route and the /api/cron/freshness-audit cron), validate-data (WARN wiring), and next.config.mjs, which derives the outputFileTracingIncludes entry for every field-stamped artifact from this file so both freshness functions are deployed with the files they open at runtime. See docs/WAYFINDER_LIVE_DATA.md for the cadence table and activation matrix. Keep entries in lockstep with vercel.json, app/api/cron, remaining workflows, and refresh scripts.",
  "version": 1,
  "generatedBy": "infra/live-data-wayfinder",
  "datasets": [
    {
      "id": "pint_prices",
      "label": "London pint-price dataset",
      "class": "episodic",
      "artifact": "public/data/pint_prices_app_dataset.json",
      "stamp": { "kind": "literal", "value": "2026-07-03T12:00:00Z", "consumedBy": "lib/dataFreshness.ts PINT_DATASET_OBSERVED_AT (build-time import; this stamp is the single source of truth for the pint dataset's collection date — the TS constant is derived from it, never hand-authored)" },
      "cadence": "episodic — re-collected by hand, then re-exported",
      "stalenessBudgetHours": 2160,
      "refreshWorkflow": "manual (npm run export:data → canonicalize:venues → build:slim)",
      "gate": "none (bundled static; keyless)"
    },
    {
      "id": "price_updates",
      "label": "Cheapest-pint baseline price updates",
      "class": "episodic",
      "artifact": "public/data/price_updates/latest.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "human-gated reviewed publish; no machine schedule until real first-party parsers ship",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "manual reviewed publish via npm run refresh:prices -- --open-pr. The retired scheduled retrieval route had only stubbed parsers and could never advance this served file. While parsers are stubbed the committed envelope stays empty and its generatedAt names the same collection day as the bundled pint dataset (2026-07-03), not a fresher-looking date with no rows behind it.",
      "gate": "none to run; first-party parsers are STUBBED (fetchFromSource returns []), so scheduled retrieval remains an honest no-op until a real parser retrieves valid rows AND a human publishes them"
    },
    {
      "id": "drink_price_updates",
      "label": "Per-drink price updates",
      "class": "episodic",
      "artifact": "public/data/drink_price_updates/latest.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "reviewed first-party observations; maximum current-price age is 14 days",
      "stalenessBudgetHours": 336,
      "refreshWorkflow": "Manual reviewed publish. The Drink price refresh workflow is retrieval-only while the allowlisted source exposes no web prices.",
      "gate": "Only allowlisted first-party source (Wetherspoons) has no per-drink web prices. Never restamp old rows after a zero-row retrieval."
    },
    {
      "id": "food_price_updates",
      "label": "Food-menu price updates",
      "class": "episodic",
      "artifact": "public/data/food_price_updates/latest.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "episodic — harvested by hand (no workflow)",
      "stalenessBudgetHours": 1440,
      "refreshWorkflow": "manual (menu harvest lane)",
      "gate": "FIRECRAWL_API_KEY for menu scraping harvest"
    },
    {
      "id": "night_signals",
      "label": "Night signals (offline-reviewed claims)",
      "class": "episodic",
      "artifact": "public/data/night_signals/latest.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "human-gated approved publish; no machine schedule",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "manual approved publish via npm run refresh:night-signals -- --open-pr; Vercel candidate ingestion never publishes reviewed claims",
      "gate": "Every claim passes human review before it ships, so this snapshot advances only on an approved publish. EXA_API_KEY arms the separate Vercel candidate-ingestion feed; it does not create a machine cadence for reviewed night_signals."
    },
    {
      "id": "night_signal_candidates",
      "label": "Night Signal candidate ingestion (Exa sweep, pending review)",
      "class": "cron",
      "artifact": null,
      "stamp": { "kind": "store", "feedKey": "night_signal_candidates" },
      "cadence": "daily, 05:15 UTC via Vercel cron",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "Vercel cron GET /api/cron/refresh-night-signals sweeps Exa for dated, attributable London pub buzz and stamps feed_freshness (durable store, migration 0047 plane). Store-only, no committed artifact: /api/freshness and the freshness-audit cron both read the store's real observedAt directly (never a disk fallback, because none exists), and scripts/check_freshness.mjs queries the same table dependency-free. A store the checker cannot reach reports unmeasurable, never fresh and never stale. Hard staleness-budgeting an artifact-less cron feed is a registry-model follow-up. This feed reports when INGESTION last ran, not that any claim shipped.",
      "gate": "EXA_API_KEY (present in Vercel Preview + Production). Keyless = safe no-op. Candidates stay PENDING; durable candidate persistence and a store-based review surface remain follow-up work. Reviewed night_signals still advances only through an approved human publish."
    },
    {
      "id": "weather",
      "label": "Night-area weather cache",
      "class": "cron",
      "artifact": "public/data/weather/latest.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "every 6h via Vercel cron (0 */6 * * * UTC); observedAt is store-backed when migration 0047 is applied, else the committed file",
      "stalenessBudgetHours": 48,
      "refreshWorkflow": "Vercel cron GET /api/cron/refresh-weather → durable weather_snapshots store (read store-first via lib/weatherSnapshots.server); committed file is the pre-migration/degraded fallback. GitHub Actions retired.",
      "gate": "none (Open-Meteo is keyless — the weather feed always runs)"
    },
    {
      "id": "whats_on",
      "label": "What's-On (sport / quiz / deals / music / events)",
      "class": "cron",
      "artifact": null,
      "stamp": { "kind": "store", "feedKey": "whats_on" },
      "cadence": "daily full What's-On refresh via Vercel cron (30 5 * * * UTC); durable rows are preferred and bundled files remain the fallback",
      "stalenessBudgetHours": 48,
      "refreshWorkflow": "Vercel cron GET /api/cron/refresh-whats-on refreshes Ticketmaster/Skiddle events plus bounded quiz, deal, music, and sport lanes into the durable whats_on_listings store (read store-first via lib/whatsOnListings.server; bundled public/data/whats_on files are the fallback). GitHub workflows remain as a secondary recovery path when Actions billing returns.",
      "gate": "TICKETMASTER_API_KEY and/or SKIDDLE_API_KEY for official event rows (Skiddle also needs written commercial approval and SKIDDLE_BRAND_ASSET_PRESENT). The quiz lane is bounded to Question One pages; deal, music, and sport lanes use their existing first-party definitions and bundled venue inputs. A failed lane leaves its prior durable rows unchanged. Live layer also blends CityMCP at request time."
    },
    {
      "id": "whats_on_sport_fixtures",
      "label": "What's-On sport fixtures (derived screenings)",
      "class": "episodic",
      "artifact": "public/data/whats_on/sport_fixtures.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "daily re-derivation via Vercel cron (30 5 * * * UTC); bundled fixtures remain the fallback",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "Vercel cron GET /api/cron/refresh-whats-on re-derives sport rows into durable whats_on_listings; scripts/whatson/sportFixtures.mjs remains the local and GitHub recovery path for the bundled fallback artifact",
      "gate": "none (bundled static; keyless). Past-dated rows are never served: lib/whatsOn.ts filterNotPast guards the serving seam in lib/whatsOnStore.ts, so a stale seed degrades to zero served fixtures rather than surfacing a played match."
    },
    {
      "id": "events_live_eventbrite",
      "label": "Eventbrite own-org events (live, Tonight surface)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — fetched per request from eventbriteapi.com, cached in-process (5 min TTL per instance, never disk-cached)",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/events/tonight via lib/events/eventbrite)",
      "gate": "EVENTBRITE_API_TOKEN. Capability-limited: Eventbrite removed public event search in 2019, so a private token can only surface the authenticated account's OWN organizations' events. Contributes 0 rows until the owner creates events under that account; lights up with no code change."
    },
    {
      "id": "pint_index_snapshot",
      "label": "London Pint Index (borough medians)",
      "class": "user-cadence",
      "artifact": "public/data/pint_index_snapshot.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "event-sourced — grows as confirmed Pint Drops / official-publisher observations arrive",
      "stalenessBudgetHours": 2160,
      "refreshWorkflow": "user-cadence (the growth loop IS the refresh mechanism)",
      "gate": "none; only confirmed Pint Drops + official-publisher / open-data sources are eligible"
    },
    {
      "id": "late_food_evidence",
      "label": "Late-food evidence",
      "class": "episodic",
      "artifact": "public/data/late_food_evidence.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "episodic — evidenced by hand per Night Area",
      "stalenessBudgetHours": 2160,
      "refreshWorkflow": "manual (evidence curation)",
      "gate": "none"
    },
    {
      "id": "pubmaxxing_seed",
      "label": "PUBMAXXING all-drinks / history seed import",
      "class": "episodic",
      "artifact": "public/data/pubmaxxing_seed_snapshot.json",
      "stamp": { "kind": "field", "pointer": "sourceImportedAt" },
      "cadence": "episodic — imported from the sibling pubmaxxing repo",
      "stalenessBudgetHours": 8760,
      "refreshWorkflow": "manual (npm run build:pubmaxxing-seed)",
      "gate": "none"
    },
    {
      "id": "wetherspoons_directory",
      "label": "Wetherspoons venue-presence directory (OSM)",
      "class": "episodic",
      "artifact": "public/data/wetherspoons/pubs.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "episodic — OSM / directory harvest",
      "stalenessBudgetHours": 4320,
      "refreshWorkflow": "manual (npm run fetch:city-pubs / scripts/fetch_wetherspoons_pubs.mjs)",
      "gate": "FIRECRAWL_API_KEY for the directory refresh path; OSM Overpass is keyless"
    },
    {
      "id": "tfl_last_train",
      "label": "TfL last-train / last-drink (live)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — fetched per request from api.tfl.gov.uk, never disk-cached",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/last-train)",
      "gate": "none (keyless); TFL_APP_KEY only raises rate limits"
    },
    {
      "id": "tfl_nearby_buses",
      "label": "TfL nearby bus departures (live)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live: nearby stops and per-stop arrivals fetched per request from api.tfl.gov.uk through the shared lib/tflClient.server.ts client, never disk-cached. Bounded fan-out: bus stops inside a 500 m straight-line radius, capped at 4, each asked concurrently inside one 15 s route budget. A prediction older than 2 minutes, undated, already due, or over an hour away is dropped, and an unreachable, malformed, empty or stale-only read answers unavailable rather than reporting that no bus is coming.",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/nearby-bus-departures via lib/nearbyBusDepartures)",
      "gate": "none (keyless); TFL_APP_KEY only raises rate limits"
    },
    {
      "id": "tfl_disruption",
      "label": "TfL material disruption (night-shaping layer)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — TfL Line Status fetched server-side and revalidated every 5 min (Next data cache), then filtered to material disruptions (suspensions, severe delays, planned closures overlapping tonight 17:00-02:00 London) touching the active night patch. No disk artifact; nothing renders when clear.",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request with 5-min server revalidate (app/api/tfl-disruption via lib/tflDisruption)",
      "gate": "none (keyless); TFL_APP_KEY only raises rate limits"
    },
    {
      "id": "food_hygiene",
      "label": "FSA food hygiene ratings (FHRS)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — fetched per request from api.ratings.food.gov.uk, cached in-process (24h TTL per instance, never disk-cached)",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/hygiene)",
      "gate": "none (keyless open API; requires the x-api-version: 2 header)"
    },
    {
      "id": "citymcp",
      "label": "CityMCP London (buzz / status / journey / places)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — proxied per request with short in-process TTLs (3–10 min)",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/citymcp/*)",
      "gate": "none (keyless upstream); buzz digest quality depends on CityMCP's own EXA-backed enrichment"
    },
    {
      "id": "police_night_context",
      "label": "Night calm context (data.police.uk street-level crime)",
      "class": "live",
      "artifact": null,
      "stamp": null,
      "cadence": "live — fetched per request from data.police.uk, aggregated to a coarse Night Area and cached per area per published month (~12h in-process TTL). Upstream publishes ~2 months in arrears; the latest available month is read from crime-last-updated.",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "live-per-request (app/api/night-calm)",
      "gate": "none (keyless open data). Surfaces one calm band only, never crime counts or per-street detail."
    },
    {
      "id": "tfl_lines",
      "label": "TfL line geometry (map basemap)",
      "class": "static",
      "artifact": "public/data/tfl_lines.json",
      "stamp": null,
      "cadence": "static — line geometry changes rarely",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "manual",
      "gate": "none"
    },
    {
      "id": "london_pois",
      "label": "London POIs (map basemap)",
      "class": "static",
      "artifact": "public/data/london_pois.json",
      "stamp": null,
      "cadence": "static — curated geographic reference",
      "stalenessBudgetHours": null,
      "refreshWorkflow": "manual",
      "gate": "none"
    },
    {
      "id": "heritage_listings",
      "label": "Listed-building heritage facts (Historic England NHLE)",
      "class": "episodic",
      "artifact": "public/data/heritage_listings.json",
      "stamp": { "kind": "literal", "value": "2026-07-18T00:00:00Z", "consumedBy": "committed dataset; the NHLE point layer refreshes daily upstream but statutory listing status changes rarely, so this is re-ingested episodically" },
      "cadence": "episodic — re-run scripts/build_heritage_listings.mjs against the live NHLE FeatureServer",
      "stalenessBudgetHours": 8760,
      "refreshWorkflow": "manual (node scripts/build_heritage_listings.mjs)",
      "gate": "none (Historic England NHLE open data is keyless; Open Government Licence v3.0, attribution required)"
    },
    {
      "id": "historic_pubs",
      "label": "Historic pubs index (heritage cache × venue dataset JOIN)",
      "class": "episodic",
      "artifact": "public/data/historic_pubs.json",
      "pack": true,
      "stamp": { "kind": "literal", "value": "2026-07-18T00:00:00Z", "consumedBy": "Regenerated when scripts/build_historic_index.mjs runs after heritage_cache.json or pint_prices_app_dataset.json changes. `pack: true` opens the file in both freshness readers, so a missing, unreadable or empty index is its own audit finding rather than a literal stamp reading as fresh." },
      "cadence": "episodic — node scripts/build_historic_index.mjs",
      "stalenessBudgetHours": 2160,
      "refreshWorkflow": "manual (node scripts/build_historic_index.mjs)",
      "gate": "none (deterministic JOIN of committed heritage_cache.json + pint_prices_app_dataset.json)"
    },
    {
      "id": "area_news",
      "label": "Area news (fresh-facts layer)",
      "class": "snapshot",
      "artifact": "data/area_news.json",
      "stamp": { "kind": "field", "pointer": "generatedAt" },
      "cadence": "snapshot - npm run refresh:area-news",
      "stalenessBudgetHours": 504,
      "refreshWorkflow": "manual (KEENABLE_API_KEY optional; npm run refresh:area-news, then node scripts/build_area_news_matches.mjs when venue badges need rebuilding)",
      "gate": "Keenable search_web_pages + fetch_page_content; a provider failure or zero valid facts leaves the prior dataset unchanged. Vercel cron cannot write this committed file, so refresh runs locally or in a review workflow."
    },
    {
      "id": "persona_drinks",
      "label": "Persona drinks (reported / canon drink orders for the Drink like lens)",
      "class": "episodic",
      "artifact": "data/persona_drinks.json",
      "stamp": { "kind": "literal", "value": "2026-07-18T12:00:00Z", "consumedBy": "committed snapshot; bundled into the app via lib/personaDrinks.ts (webpack inlines the JSON). This literal is the crawl's collection date, the single source of truth for how fresh the persona set is." },
      "cadence": "episodic snapshot — re-run the persona crawl (Exa + Firecrawl), drop the new JSONL in as data/persona_drinks.source.jsonl, then node scripts/build_persona_drinks.mjs",
      "stalenessBudgetHours": 2160,
      "refreshWorkflow": "manual (node scripts/build_persona_drinks.mjs)",
      "gate": "none to check (bundled static). A refresh crawl needs EXA_API_KEY and FIRECRAWL_API_KEY."
    }
  ]
}
