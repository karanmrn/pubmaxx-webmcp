# Sol sync: state of the world, 2026-07-22

Written by Fable for Sol. Everything Fable-side executed since your last wave, the corrections that affect your model of the codebase, and your next wave. Owner (Karan) has seen none of the open decisions below except where marked.

## What Fable executed (merged to main, live on pubmaxxing.com)

### Unknowns programme + GNHF waves (2026-07-21, ~30 PRs, #464-#488)
- Brand: the Clink mark rolled across favicon/ico/icons/OG (NOTE: being re-decided, see X-mark below).
- Entry: `lib/entryDecision.ts` seam (deep link > native first-run > session revisit > shell cold start > web default), owner amendment: wordmark tap reaches the landing page.
- Ops: `lib/opsFreeze.ts` social write freeze + runbook (#470), sponsorship fence + policy doc (#479), www to apex 308 (#475), night crawl mode (#472).
- Data planes: patch capabilities (#481), TfL disruption line (#482), fact-claim conflict model authority > freshness > corroboration > confidence (#483), visit reports + summaries (#484), Vercel cron plane weather/whats-on/freshness (#485, dormant until owner sets CRON_SECRET), story redaction choke (#486), alt-text belt (#487), venue operator rail (#488).
- Write-surface certification literal is now 69. Bump in the same commit as any new POST/PATCH/DELETE route.

### Mobile QA wave (2026-07-22, #489-#491)
- #489 PWA home-tap fix. Root cause: manifest `start_url: /tonight` means the installed app never mounts AppEntryRoute at "/", so the session-entry flag was never stamped and the first wordmark tap bounced back to /tonight. New `components/native/EntryBootStamp.tsx` in the root layout consumes the entry on any non-root boot. Verified live.
- #490 sharp CVE unblock. New libvips advisories against sharp <0.35.0 made `npm audit --audit-level=high` (the last verify step) fail EVERY build on EVERY branch. Fixed with direct dep ^0.35.3 + `overrides: { sharp: "$sharp" }`. If your branch predates this merge and fails only on the audit tail, merge main in.
- #491 road-following crawl routes. `/api/walk-route` (GET, always-200 fail-soft; OpenRouteService foot-walking server-side behind ORS_API_KEY; keyless default = straight line; per-leg cache `walk_route_legs` with memory fallback), map repaint (solid = routed, dashed = approximate, `--route-line` tokens crimson/coral), PubMapCanvas straight-first-then-upgrade, plan-page "See the walking route" deep link via `lib/crawlUrl.ts`. Verified live end to end.

### Corrections to your model
1. Vercel builds run the FULL ci (validate-data, lint, typecheck, coverage vitest, npm audit) and gate merges. GitHub Actions stays retired (owner: billing).
2. Route tests MUST mock `@/lib/serverEnv` (assertServerEnv no-op) and usually pin the supabase seam, or they pass locally and die inside Vercel builds. See `__tests__/opsFreeze.test.ts` lines 11-15 for the canonical shape.
3. `vitest.setup.ts` now also strips EXA_API_KEY, FIRECRAWL_API_KEY, TICKETMASTER_API_KEY, SKIDDLE_API_KEY, ORS_API_KEY. Keyless behaviour is the test baseline; stub keys explicitly.
4. Map is MapLibre GL 5.24 under `components/map/canvas/` (custom scene builder). Issue #229 (MapLibre migration) is moot.
5. X-mark redesign in flight: five candidates in `docs/design-explorations/x-mark-v2/` awaiting the owner's pick. Do NOT invest in Clink-specific brand surfaces until that lands.

## Owner queue (nothing you or Fable can advance)
Apply migrations in order: 0038, 0044, 0045, 0046, 0047_cron, 0047_alt_text, 0048, 0049_walk_route_legs, 0050 (#537), 0051_analytics_event_receipts (#456), 0052_web_push_subscriptions (#457), 0053_push_identity_join (#459). Set CRON_SECRET and ORS_API_KEY in Vercel env (DONE 2026-07-22). Search Console reindex. X-mark verdict.

URGENT since #537 merged: storeBackend now FAILS CLOSED on missing durable writes. areaDemand, ratings, venueOperators, visitReports, realtime writes hard-error in production until the migration backlog above is applied. This is correct security posture, but the apply queue is now functional, not housekeeping.

## Sol's next wave

Your open PRs come first: rebase #456 / #457 / #459 / #462 onto current main (post-#490; expect only the audit fix and walk-route/entry files, no overlap with your lanes).

New tickets in YOUR files (Fable stayed out of them):
- S1 `components/plan/PlanComposer.tsx`: "Lock it in" shows only the native browser tooltip when "Your name" is empty. Add the house inline error (the `planComposer__error` pattern already in the file) so the submit never looks dead. Found in live mobile QA.
- S2 Plan intake precedence follow-ups from Fable's two-axis review of your wave (auth lockout + intake precedence items you already started in your pubmax-fable-* worktrees; land them).
- S3 `app/api/plans/generate`: expose per-stop walking minutes by consuming `/api/walk-route` leg durations server-side once ORS_API_KEY lands (contract: keyless = keep current straight-line estimates; never block generation on routing).
- S4 Web push continuation (your Lane B): plan-scoped push targeting still needs the identity join; push tokens register pre-auth with no user/plan identity.

Fable's parallel wave (do not touch): `/u/[handle]` and page-metadata sweep, walk-route hardening (timeout + per-IP limiter) + honest leg labels (T7), plan-page static route mini-map (T8-lite). All outside your lane files.

Coordination rule unchanged: Fable reviews and merges on green Vercel; separate worktrees; no shared-file collisions. Flag conflicts in FABLE_HANDOFF.md.

## Live wave log (v1 push, updated as waves land)

- Wave 1 (merged): #489 home-tap fix, #490 sharp CVE unblock, #491 road-following routes.
- Wave 2 (merged): #492 metadata sweep, #493 plan-page route mini-map, #494 walk-route hardening (T7 label groundwork is ready for you: lib/routeLegs.ts withRoutedDistances / distanceBasis; MobilePlanActivation + plans/generate wiring is yours).
- Wave 3 (in flight): landing page v1 (app/page.tsx + components/landing, Fable lane) + code-level mobile v1 audit (read-only) covering the journey surfaces NOT occupied by you or the Cursor draft.
- Coordination note: Cursor draft #495 currently occupies app/tonight/TonightClient.tsx, components/map/TonightLane*, PlanComposer, PlanCrew, app/plan/[id]/page.tsx, plan.css, check-ins route. Fable lanes are staying out of those files; you should too until it lands or is closed. Its check-ins auth change overlaps your security review list - reconcile before merging yours.
- Wave 4 (planned): implementation lanes generated from the audit findings (mobile polish P0/P1s + desktop coherence). Target: v1-credible mobile + desktop today.

## Fable review verdicts: Sol's open PRs + Cursor drafts (2026-07-22, second review)

SOL (#456/#457/#459/#462), all NEEDS-REBASE. Every green check is stale: all four branches predate the sharp CVE fix (#490) and will fail the npm audit gate on their next build. Hand-off list:
1. Merge origin/main into all four (expect conflicts in vitest.setup.ts / serverEnv.ts / supabase.ts on #456).
2. Renumber migrations to avoid main collisions. CORRECTION (2026-07-23): 0050 is now TAKEN by merged #537 (20260722123000_0050_api_only_social_reads). New mapping: 0045_analytics_event_receipts -> 0051, 0046_web_push_subscriptions -> 0052, 0047_push_identity_join -> 0053 (fresh timestamps). Do not use 0050.
3. #459: write-surface cert literal is stale (bumped 64->66, truth is 69 pre-your-routes; recount post-rebase).
4. #457: expect a small sw.js conflict with merged #512 (offline plan cache; additive, different regions, a fence test asserts the plan-cache wiring so keep it intact).
5. Land order suggestion: #457 -> #459 -> #462 -> #456.
6. Still unaddressed from the old fix-list: auth lockout. Confirm intake precedence (described night beats chips) is truly covered by #462 or ticket it.

CURSOR (#495-#498): #498 READY after mechanical rebase behind Fable's fixpack+voice lanes (now merging). #495 NEEDS-CHANGES: one product-copy em dash ("Invite copied — paste it...") violates house law; rebase over #493; resolve PlanComposer overlap with #496. #496 code is sound but edits Sol lane files while #459 is open on the same auth surfaces: Sol must ack or reconcile before merge. #497 MUST land after Fable's ORS daily-budget lane and route fetchWalkLegRoute through the global budget counter, or Friday plan generation drains ORS quota outside the cap; also expect walkRouteProvider.ts conflicts. Newer drafts #501-#505/#507 not yet reviewed.

Merge order of record: Fable in-flight lanes -> #498 -> #495 -> #496/#497 after Sol reconciliation. Cert literal stays 69 across all Cursor drafts (none add mutating routes).

## Fable review: newer Cursor drafts #501-#505, #507

- #505 (match-the-group post-join prefs): READY-TO-UNDRAFT after a fresh CI run. Cleanest of the six; correct hard-constraint semantics (strictest budget, zero-proof promoted).
- #502 (map skeleton-first): READY after rebasing behind the #501 consolidation (shared PubMap.tsx).
- #501 (map chrome slim): consolidate with the un-PR'd cursor/map-fab-polish branch into ONE PR rebased over #513, close the other.
- #507 (Stories active state): right behavior, mechanical rebase over #506/#513 (nav files rewritten); must keep #513's spotlight-column regression test green.
- #504 (urgency badges + walk totals): badges PASS the taste doctrine (purely time-derived, no fake scarcity). BUT lib/planRouteTotalsClient.ts duplicates #497's server-side walking totals; the server path wins per the road-route design doc (and is budget-metered post-#515). Split the PR: badges + night-profile land; client totals dropped or rebased onto #497 output.
- #503 (invite privacy preview): SPEC NOT MET despite a correct DTO: gating is client-side only, venue names still ship in the pre-join SSR HTML/React payload. True server-side redaction trades off against the offline plan cache (#512 relies on stops in SSR HTML) and the HttpOnly member-token path scoping. THIS IS A DESIGN DECISION: options are (a) server-redacted pre-join page + accept offline cache only post-join, (b) accept cosmetic redaction and document the leak, (c) separate pre-join route. OWNER/SOL RULING NEEDED. Also: drop the unexplained package-lock.json hunk (strips sharp libc fields; lockfile regenerated with a different npm) before any merge.
- Cross-cutting: all six predate today's 14-merge burst; every green check is stale, fresh builds required. No cert-literal impact. #501/#507 add e2e Playwright specs that no CI gate runs; dead weight unless an e2e runner is planned.
