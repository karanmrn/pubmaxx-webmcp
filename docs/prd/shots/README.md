# PRD screenshot baseline

These images are the **2026-08-08** visual baseline for the UI/UX fix wave
(`docs/prd/UI_UX_FIX_PRD.md`).

- `d-*` — desktop, 1440px wide
- `m-*` — mobile, 390px wide

Captured from live production after the fix-day deploy.

When a later PR changes a surface named in the PRD, refresh the matching shot
in the same PR so this folder stays the visual state of record.

Finding **7** (screenshot baseline) is bundle-ok with any other item in the
wave.
